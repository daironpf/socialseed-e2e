"""Test: Check Availability

Run: pytest services/booking_demo/modules/03_availability.py
"""

from datetime import datetime, timedelta
from socialseed_e2e import BasePage

BASE_URL = "http://localhost:5006"

def run(page: BasePage):
    today = datetime.now().date()
    start_date = today.strftime("%Y-%m-%d")
    response = page.get("/api/services/svc_1/availability", params={"start_date": start_date})
    assert response.status == 200
    data = response.json()
    assert "slots" in data
    assert len(data["slots"]) > 0
    return response

if __name__ == "__main__":
    from playwright.sync_api import sync_playwright
    
    def main():
        with sync_playwright() as p:
            browser = p.chromium.launch()
            page = browser.new_page()
            base_page = BasePage(base_url=BASE_URL)
            base_page.set_page(page)
            run(base_page)
            browser.close()
    
    main()